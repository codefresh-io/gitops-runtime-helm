{{- $context := deepCopy . }}
{{- $_ := set $context "Values" (deepCopy (get (index .Values "global") "event-reporters")) }}
{{- $_ := set $context.Values "global" (deepCopy (get .Values "global")) }}
{{- $valuesOverrides := index .Values "event-reporters" "runtime-event-reporter" }}
{{- $_ := mergeOverwrite $context.Values $valuesOverrides | set $context "Values" }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "runtime-event-reporter.serviceAccountName" $context }}
  labels:
    {{- include "runtime-event-reporter.labels" . | nindent 4 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "runtime-event-reporter.fullname" . }}
  labels:
    {{- include "runtime-event-reporter.labels" . | nindent 4 }}
rules:
  - apiGroups:
    - '*'
    resources:
    - '*'
    verbs:
    - '*'
  - nonResourceURLs:
    - '*'
    verbs:
    - '*'
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "runtime-event-reporter.fullname" . }}
  labels:
    {{- include "runtime-event-reporter.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "runtime-event-reporter.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "runtime-event-reporter.serviceAccountName" $context }}
    namespace: {{ .Release.Namespace }}
